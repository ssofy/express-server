import { ServerConfig } from "../ServerConfig";
import { Bindings } from "../Bindings";
declare const _default: (config: ServerConfig, bindings: Bindings) => import("express-serve-static-core").Router;
export default _default;
